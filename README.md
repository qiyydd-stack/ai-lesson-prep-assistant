# AI备课辅助工具 Demo

基于 `cwhhwc/AI-lesson-plan-writing` 的中文教案产品方向做瘦身改造，保留“教师输入需求、AI 生成结构化教案”的核心场景，移除登录、历史记录、文档库和复杂导出流程。

## 功能

- 输入学科、年级、章节、课时、教学类型、教学风格和学情补充
- 支持本地账号注册和登录，业务接口需要登录后使用
- 教学类型支持严谨讲授课、探究启发课、考前复习课、一对一家教、习题课、实验实践课、公开展示课和自定义
- 教学风格支持严谨清晰、启发引导、互动活跃、温和陪伴、高效冲刺、分层照顾、故事化情境和自定义
- 生成前可设置教案详略、PPT 页数，并用标签选择板书设计、课堂评价、分层作业等生成项
- 支持粘贴或导入 `.txt/.md` 校本模板，作为生成约束
- 生成任务进入轻量任务队列，记录生成教案、PPT 预览、PPTX 导出和局部重写的状态
- 将生成后的 Markdown 教案自动拆成可编辑内容块，编辑后同步给复制、PPT 生成和局部重写
- 支持按内容块复制教学目标、课堂互动问题、课后作业题等片段，方便二次复用
- 支持校本资源库 / RAG：上传校本题库、优秀教案、课标摘录、教研材料后，系统会按当前学科、年级、章节检索相关片段注入生成上下文
- 所有关键文字输入区支持上传参考资料，支持 `.txt`、`.md`、`.docx`、`.xlsx`、`.xls`、`.csv` 和常见图片格式
- Word/Excel/文本资料会自动提取文字；练习册、试卷等图片优先调用本地 RapidOCR 识别题目，必要时再走支持视觉能力的 OpenAI 兼容模型
- 支持局部重新生成教学目标、重难点分析、教案框架、课堂互动问题和课后作业题
- 可在前端页面配置 OpenAI 兼容接口的 API Key、Base URL 和模型名
- 调用本地后端代理，以流式方式生成 Markdown 备课方案
- 输出教学目标、重难点分析、教案框架、课堂互动问题、课后作业题
- 可基于已生成教案生成教学 PPT 预览，并下载可编辑 `.pptx`
- 支持上传带 `{{deck_title}}`、`{{slide_1_title}}` 等占位符的 `.pptx` 模板来导出 PPT；`.ppt` 需先另存为 `.pptx`
- 支持示例填充、清空、复制完整结果或单个内容块

## 运行

```bash
npm install
python -m pip install rapidocr-onnxruntime
Copy-Item .env.example .env
npm run server
npm run dev
```

可以直接在页面右上角“模型配置”中填写 API Key、Base URL 和模型名。配置会保存在当前浏览器本地，生成时发送给本地后端代理。

也可以在 `.env` 中配置默认值：

```env
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
PORT=3001
AUTH_TOKEN_SECRET=change_this_for_deployment
```

前端默认地址：`http://localhost:5173`

后端默认地址：`http://localhost:3001`

图片资料优先使用本地 RapidOCR，不依赖文本模型的视觉能力；如果本地 OCR 不可用，才会尝试支持视觉输入的模型接口。`.exe`、脚本和安装包等可执行文件不会被解析。

## 验证

```bash
npm test
npm run build
```

## 说明

本 Demo 面向教学场景演示，已包含本地账号登录，但不包含组织权限、学校数据接入、审计和内容安全工作流。前端填写 API Key 适合本地演示，不建议在公网环境开放使用。正式落地时建议增加学校侧模型网关、敏感词与事实性检查、教师确认发布流程、校本资源库和使用日志。

当前账号系统是本地 Demo 版本，用户数据保存在后端 `data/users.json`，密码使用 `scrypt` 哈希后落盘，`data/` 不会提交到 Git。正式部署时建议替换为数据库、HTTPS、强密钥、组织权限和统一身份认证。

当前校本资源库借鉴 RAGFlow、AnythingLLM 等开源 RAG 产品的“资料入库、切块、检索、注入上下文”流程，但为了保持 Demo 简洁，采用本地浏览器存储和轻量关键词检索；生产环境建议替换为数据库、对象存储、向量检索、权限控制和资源审核流程。
